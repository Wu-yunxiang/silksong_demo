package com.example.gameobjects.effect;

/**
 * 二段跳特效 (Double Jump Effect)
 * 说明：
 * - 当角色触发二段跳时显示的视觉/特效（例如起跳时的空气粒子、音效或浮动光环）。
 */
public class DoubleJumpEffect extends Effect {

    @Override
    public void update(float deltaTime) {
        // 二段跳特效更新（占位）
    }

    @Override
    public void render() {
        // 二段跳特效渲染（占位）
    }
}
