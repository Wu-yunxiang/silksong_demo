package com.example.gameobjects.pending;

import com.example.gameobjects.GameObject;
import com.example.rendering.Renderer;

/**
 * 装饰物类（装饰性对象）。
 *
 * 说明：
 * - 表示用于场景或角色周边的装饰性对象（如旗帜、灯笼、背景道具等）。
 * - 继承自 `GameObject`，当前仅作类声明，不包含字段或方法。后续按你的指示补充。
 */
public class Decoration extends GameObject {

	@Override
	public void update(float deltaTime) {
		// 声明性实现：无具体逻辑（占位）
	}

	@Override
	public void render() {
		// 声明性实现：无具体逻辑（占位）
	}
}
